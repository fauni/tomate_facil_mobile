import 'package:flutter/material.dart';

class DetalleRecepcionScreen extends StatelessWidget {
  const DetalleRecepcionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text('Detalle de Recepcion')),
    );
  }
}