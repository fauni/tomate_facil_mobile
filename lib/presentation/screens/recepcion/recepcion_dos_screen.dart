import 'package:flutter/material.dart';

class RecepcionDosScreen extends StatelessWidget {
  const RecepcionDosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Recepcion 2'),
    );
  }
}
